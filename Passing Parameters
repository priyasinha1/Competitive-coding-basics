#include<iostream>
/*
#include<iostream>
using namespace std;
*/

// Your code goes here

void compute(int& A, int& B, int& C){ //passing argument 1 by reference using &
    A = A*A*A;
    B = B*B*B;
    C = C*C*C;
    //we equate arg1 to arg2
    //there is no return as void function
}

/*
int main()  {
    int A, B, C;
    cin>>A>>B>>C;
    compute(A, B, C);
    cout<<A<<endl;
    cout<<B<<endl;
    cout<<C<<endl;
    return 0;
}
*/
